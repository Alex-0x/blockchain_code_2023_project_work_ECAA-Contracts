
                                                    MULTISIG

aggiungere le funzioni per permettere di aggiungere e rimuovere partecipanti al multisig (addOwner, removeOwner),quindi implementare il sistema di 
votazione da parte dei membri per l aggiunta e la rimozione della nuova persona(vedere sistema votazione transazione).(COSIMO CONTROLLARE)

implementare per questo multisig l utilizzo di erc20 e erc721 (capire se implementare 1155)

treshold intero e non a percentuale

implementare funzione per aggiornare il treshold(ricordarsi che nel front end dare guidare al fatto che ha aggiunto qualcuno e puo voler aggiornare il 
treshold)

fare un mapping per address con un bool (verified ) poi implementare funzione per cambiare un owner(nel caso di perdita di chiavi) => avra un treshold 
minore del principale e settera il verified a false e oltre a questo settera un unlocktime

implementare l imHere che risettera il verified a true cosi che non si potra chiamare l executechange

implementare funzione executechange(address1,address2) che per attuarsi richiedera che l unlocktime e il booleano siano verificati questa funzione 
sostituira l address1 ,cancellandolo dall arrey degli owner , con l address 2 e lo pushera nell arrey owner.
 